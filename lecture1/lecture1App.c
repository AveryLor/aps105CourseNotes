#include <stdio.h> // This is the standard input output file that allows the program to print to the console

int main() { // The program starts in main, it finds the main function and runs that function

    int integer =  10; 

    printf("Hello, World!\n"); // This is a function that prints to the console
    printf("%d", integer);
    return 0; // Will print 0 if there is no error, otherwise it will print a non-zero number
}